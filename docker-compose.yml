version: "3"
services:
  frontend:
    image: keemba/docker-client
    volumes:
      - /app/node_modules
      - ./client:/app
    stdin_open: true
    mem_limit: 128m

  nginx:
    restart: always
    image: keemba/docker-nginx
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend

  backend:
    image: keemba/docker-server
    volumes:
      - /app/node_modules
      - ./server:/app
    mem_limit: 128m
    environment:
      POSTGRES_HOST: database-1.cn48sgko2q4b.ap-northeast-2.rds.amazonaws.com
      POSTGRES_USER: sub_kimba
      POSTGRES_ROOT_PASSWORD: tntls1!
      POSTGRES_DATABASE: myapp
      POSTGRES_PORT: 5432
