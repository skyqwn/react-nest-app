services:
  postgres:
    image: postgres:15
    restart: always
    volumes:
      - ./data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
  # environment:
  #   - DB_HOST=${DB_HOST}
  #   - DB_PORT=${DB_PORT}
  #   - DB_USERNAME=${DB_USERNAME}
  #   - DB_PASSWORD=${DB_PASSWORD}
  #   - DB_NAME=${DB_NAME}
